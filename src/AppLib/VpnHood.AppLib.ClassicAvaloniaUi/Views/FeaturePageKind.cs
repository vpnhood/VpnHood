namespace VpnHood.AppLib.ClassicAvaloniaUi.Views;

// The card a feature page fills: the steps as a rule, and the two pages with text of their own.
public enum FeaturePageKind
{
    Steps,
    CloakMode,
    PrivateDnsError
}
