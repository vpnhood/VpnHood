<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<IsPackable>false</IsPackable>	<!-- application, not a NuGet package -->
		<Product>VpnHood! CONNECT</Product>
		<Description>VpnHood Windows Client App</Description>
		<OutputType>WinExe</OutputType>
		<TargetFramework>net10.0-windows10.0.26100.0</TargetFramework>
		<UseWPF>true</UseWPF>
		<UseWindowsForms>False</UseWindowsForms>
		<AssemblyName>VpnHoodConnect</AssemblyName>
		<ApplicationIcon>Resources\VpnHood.ico</ApplicationIcon>
		<EnforceCodeStyleInBuild>false</EnforceCodeStyleInBuild>
		<SupportedOSPlatformVersion>10.0.17763.0</SupportedOSPlatformVersion>
		<ApplicationManifest>Properties/app.manifest</ApplicationManifest>
	</PropertyGroup>

	<PropertyGroup Condition="'$(Configuration)'=='Release'">
		<RuntimeIdentifiers>win-x64</RuntimeIdentifiers>
	</PropertyGroup>

	<PropertyGroup>
		<StartupObject></StartupObject>
		<!-- Anchored to THIS FILE, not to $(SolutionDir): that property is empty whenever the project
		     is built on its own (dotnet build <csproj>, an IDE single-project build), which silently
		     embedded nothing and left the app running on whatever in-code defaults happened to exist. -->
		<VhUserDir Condition="'$(VhUserDir)' == ''">$(MSBuildThisFileDirectory)..\..\..\..\.user</VhUserDir>
	</PropertyGroup>

	<!-- Optional private app settings (from the external ".user" folder) embedded as resources.
	     Conditional so a fork without them still builds; AppConfigs reads them at startup. A fork
	     that has none gets no PortalBaseUri, and App.cs then builds no account provider at all. -->
	<ItemGroup>
		<EmbeddedResource Include="$(VhUserDir)\VpnHoodConnect\appsettings.json"
			LogicalName="AppSettings.json"
			Condition="Exists('$(VhUserDir)\VpnHoodConnect\appsettings.json')" />
		<EmbeddedResource Include="$(VhUserDir)\VpnHoodConnect\appsettings.$(Configuration).json"
			LogicalName="AppSettings_Environment.json"
			Condition="Exists('$(VhUserDir)\VpnHoodConnect\appsettings.$(Configuration).json')" />
	</ItemGroup>

	<!-- Default access key, embedded as its own resource so it can be sourced from a GitHub secret.
	     Windows is a direct download, so it shares the 'web' distribution key with the Android web APK.
	     The configuration-specific file wins (Debug embeds access_key_default_web.Debug.txt), otherwise
	     the base access_key_default_web.txt is used. Both are optional here: a fork without them falls
	     back to the in-code default in AppConfigs, while a real (strict) publish is gated by
	     Assert-DefaultAccessKey in pub/lib/AppPublishConfig.ps1. -->
	<ItemGroup>
		<EmbeddedResource Include="$(VhUserDir)\VpnHoodConnect\web\access_key_default_web.$(Configuration).txt"
			LogicalName="access_key_default.txt"
			Condition="Exists('$(VhUserDir)\VpnHoodConnect\web\access_key_default_web.$(Configuration).txt')" />
		<EmbeddedResource Include="$(VhUserDir)\VpnHoodConnect\web\access_key_default_web.txt"
			LogicalName="access_key_default.txt"
			Condition="!Exists('$(VhUserDir)\VpnHoodConnect\web\access_key_default_web.$(Configuration).txt') And Exists('$(VhUserDir)\VpnHoodConnect\web\access_key_default_web.txt')" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\..\AppLib\VpnHood.AppLib.Portal\VpnHood.AppLib.Portal.csproj" />
		<ProjectReference Include="..\..\AppLib\VpnHood.AppLib.Win.Common.WpfSpa\VpnHood.AppLib.Win.Common.WpfSpa.csproj" />
		<ProjectReference Include="..\Client\VpnHood.App.Client.csproj" />
	</ItemGroup>

</Project>
