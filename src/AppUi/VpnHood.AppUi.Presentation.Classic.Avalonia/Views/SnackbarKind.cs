namespace VpnHood.AppUi.Presentation.Classic.Avalonia.Views;

// The colours the web UI's showGeneralSnackbar takes: the highlight by default, the active green
// for a success, the warning orange, and the cream of the 'suppressed to' notice.
public enum SnackbarKind
{
    Highlight,
    Active,
    Warning,
    Suppress
}
